/*place global styles here */
html,
body {
	@apply h-full;
}
body {
	background-image: radial-gradient(
			at 0% 0%,
			rgba(var(--color-primary-500) / 0.33) 0px,
			transparent 80%
		),
		radial-gradient(at 98% 60%, rgba(var(--color-secondary-500) / 0.33) 0px, transparent 70%);
}


